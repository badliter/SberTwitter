import React from "react";

const Tweet = (props) => {
    const {
        tweet_id,
        user_id,
        content,
        timestamp,
    } = props.tweet;

    return (
        <div />
    );
};

export default Tweet;
